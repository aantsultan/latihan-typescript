describe('Data type', () => {
    it('should must declare', () => {
        const name = 'aant';
        const balance = 100;
        const isAgree = true;

        console.info(name);
        console.info(balance);
        console.info(isAgree);
    });
});